<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/project_graph"
    app:startDestination="@id/projectFragment">

    <fragment
        android:id="@+id/projectFragment"
        android:name="com.sinx.project.ProjectFragment"
        android:label="ProjectFragment" >

        <action
            android:id="@+id/action_projectFragment_to_project_add_new_graph"
            app:destination="@id/project_add_new_graph" />
    </fragment>
    <include app:graph="@navigation/project_add_new_graph" />
</navigation>